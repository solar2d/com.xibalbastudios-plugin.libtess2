Sources for the plugin `plugin.libtess2`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.libtess2" = {
            publisherId = "com.xibalbastudios",
        },
    },
}
```
